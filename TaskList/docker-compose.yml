version: "3.7"

services:
  api_exemplo_render:
    container_name: api_exemplo_render
    build:
      context: .
      dockerfile: Dockerfile
    image: api_exemplo_render
    ports:
      - "5218:8080"

5218